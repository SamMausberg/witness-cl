# Separate stream after a local output-schema rejection

The first prospective invocation, `prospective-92001`, stopped after two arm-episodes. Both submitted answers were correct, but the first insight reflection received HTTP 400. The native runtime rejected the generated `char{0,4096}` grammar because its repetition count exceeded the parser limit, and logged that the sampler/task slot failed to initialize. The saved trace has one unknown-usage call. Its known tokens are not a complete cost total, and this run remains failed and incomplete. No fragment episode or learned program occurred.

The next separately frozen invocation uses the same eleven executed source files, backbone, prompt, thinking settings, per-arm budgets, shared caps, eight-query action allowance and strict host validation. Its only request-format change is the existing client option `response_mode=json` for **every action and reflection in every arm**. The backend generates a JSON object without the detailed schema grammar; the actual action, memory, scalar, string and byte validators still apply unchanged. No schema or memory limit is weakened, and malformed outputs incur the existing rejection policy.

Use fresh prospective development seed 92002, reuse condition, all memories initially empty, with full history, evolving SQL-capable context and checked executable memories. The same full 48-episode-per-arm schedule and 7/8 warm gate from `STREAM_PROTOCOL.md` apply. Stop the run on missing usage, runtime failure or any resource ceiling. This remains development, not heldout or a confirmatory replication of the schema-mode screen. Keep both attempts and every earlier diagnostic.

Before this invocation, measured experiment time is 391.591767502 seconds and 143 generation requests were attempted. Known usage is 170388 tokens. The one failed request had 1489 preflight tokens and a 4096-token output reservation, so conservatively debit 5585 additional tokens for a total upper bound of 175973. This does not replace its missing usage with a measured value. With the new stream's 1800-second, 3M-token and 1200-call caps, the original 45-minute, 4M-token and 1600-call development ceilings remain respected under that conservative bound.

The external receipt is `artifacts/v9/prepilot-json-freeze.json`; the unused output directory is `artifacts/v9/prospective-json-92002`. No additional task call is hidden as setup or preflight. Sources and this protocol freeze before execution. The new study gets its own completeness, costs, score and replay; its rows are never spliced into the failed run.
