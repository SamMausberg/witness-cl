# Witness-CL continuation plan

Continue from the current Git state. Do not repeat completed experiments merely to generate activity. `RESEARCH_STATUS.md`, `EXPERIMENT.md`, `NATIVE_BENCHMARK.md`, and artifact manifests are the current checkpoint. Preserve the imported history and all failed results. Keep the GitHub repository private.

## 1. Test a bounded adaptive probe plan

The frozen one-probe regret heuristic is rejected; weak-dominance closure is only a post-hoc partial repair. Implement a separately named depth-two adaptive probe selector. For each affordable first probe, partition the exact current cover by observable outcome; choose a second affordable probe separately in each branch; evaluate the worst-branch terminal decision regret. Use library-restricted policy values and immutable plans. Recheck and debit each actually executed episode through the original checker and ledger. Hypothetical traces must never enter observed evidence or neural training.

Use an explicit node/time cap across completion generation, duplicate enumeration, partition evaluation, and paired checking. The current `max_models` bounds distinct yielded completions, not iterator work or execution time. Incomplete planning must return UNKNOWN and use a declared fallback. Compare both against original v4 and a cheap informative-zero-score fallback, so extra lookahead must justify its cost.

First require the four-world complementary-probe test to reach certified return 10 at budget zero in all worlds, preserving every old incumbent. Then use a new larger or structurally different task family and varied horizons/schedules/initializations. The 256-table binary world family and original seeds are now development data: do not call a new random seed an unseen-domain holdout. Freeze the new primary contrast and cost budget before looking at outcomes.

Kill the new rule if it cannot solve the explicit complementary-probe case, creates any false admission or budget breach under its valid contract, or fails to improve future reward over the cheap fallback and strong full-history baseline at matched total compute. Measure planning/setup/online-update time, interaction cost, storage and peak memory separately. Start with CPU only; no new GPU kernel is justified by the present bottleneck evidence.

## 2. Establish an actual native baseline before claiming native learning

The upstream checkout is outside the deliverable repo at `../../work/clbench-upstream`, pinned to `5f8c50eb1e84b2eda2ef4faff757dfc812a0ea26`. Review `integrations/clbench` and `NATIVE_BENCHMARK.md` for the tested Python 3.13 runtime and exact commands. Verify the pin before each native test. Use native Query/observe semantics and scoring; do not double-deliver feedback or expose query metadata/evaluator records to the model.

Database Exploration is the first proposed domain. Its native feedback can reveal the correct answer after an unsuccessful action; such legally delivered evidence must remain available to every baseline. Hidden answer records, database paths and private environment tables are different and must remain outside the learner. Preserve the native generic migration notice; do not invent a perfectly accurate hidden regime ID. A real task's validity is not established by setting K=2 in an adapter.

Run the unmodified upstream ICL with a fixed local backbone through a verified transport. Measure stateful and its own stateless control on the same native schedule/scorer, including invalid responses and timeout costs. Start with one actual native pilot and produce its run manifest before expanding. A mock transport plus miniature SQLite fixture validates plumbing, not the published benchmark.

Only then add typed, evidence-linked scope rules and a fixed-memory formatting control. Test stale-scope reuse, contradictory evidence, limited memory, and shadow old-scope probes whose outcomes cannot train the learner. Proposed wider target: +5 percentage points of native normalized gain over the strongest matched baseline, positive paired lower 95% bound, and at most two points protected-scope success loss. These are targets, not achieved thresholds.

## 3. Close proof-to-runtime gaps

The 51 abstract Lean statements already compile and have a complete axiom audit. The next useful formal increment is an executable finite policy interpreter and trace-cover update with a proved refinement, followed by differential testing against Python. Do not substitute more elementary inequalities for this missing implementation connection. Preserve UNKNOWN/INCONSISTENT behavior and explicit class/reset assumptions.

## 4. Test whether online training contributes

Keep the candidate pool fixed and compare the learned ranker against count-based, frozen and random order under equal generation/check budgets. Report checker yield, later reward, gradient time and retained-scope behavior. The frozen v5 experiment did not establish a positive training effect. The real pilot changed only the auxiliary ranker, never the 27B model weights. Larger parameter adaptation requires a separate falsifiable retention protocol and actual resource estimate.

## Resources, execution and reporting

The first local-model server was stopped after completion to release the GPU. The exact model hash and configuration are in `artifacts/v5/local-model-provenance.json`; weights are not in Git. Existing local compute is authorized. Do not start paid APIs, rent GPUs, publish publicly, submit a paper/leaderboard result, message others, or change visibility without a concrete user decision. Background follow-up is scheduled every two hours in this task; report substantive results, failures or decisions, and stay quiet on unchanged state.

After each meaningful increment, run affected tests and the required suite, update the claim ledger and paper when claims change, commit/push to the private repo, and record the exact next step. Retain negative findings. If native data/tool/model access blocks work, continue the independent CPU/formal tasks. Ask for a user decision only after preparing an exact run plan and measured resource requirements.
