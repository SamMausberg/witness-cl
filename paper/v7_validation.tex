\paragraph{Executable validation.} The standard Python suite passes 656 tests with 8 optional native-environment skips. C++ checks pass 432 reference cases, 200 randomized continuation cases, and 400 Python/C++ pairs with undefined-behavior sanitization. The Lean audit checks 77 accumulated statements with no unexpected axioms. Independent standard-library replay passes every held-out arm/run, reconstructing visible rows, computing gold targets with separate SQL, evaluating serialized policies independently, and checking exact gate capital, permanent alpha, policy transitions, complete panels and budget prefixes. Its receipt binds each raw file and the source/configuration freeze. It verifies recorded consistency and accounting; it does not independently measure original timings or authenticate historical execution.
